/*
 * ComponentsPage Messages
 *
 * This contains all the text for the ComponentsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ComponentsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ComponentsPage container!',
  },
});
