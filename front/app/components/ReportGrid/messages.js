/*
 * ReportGrid Messages
 *
 * This contains all the text for the ReportGrid component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ReportGrid';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReportGrid component!',
  },
});
